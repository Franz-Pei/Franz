'use strict';

var vue = require('vue');

const COffcanvasTitle = vue.defineComponent({
    name: 'COffcanvasTitle',
    props: {
        /**
         * Component used for the root node. Either a string to use a HTML element or a component.
         */
        as: {
            type: String,
            default: 'h5',
        },
    },
    setup(props, { slots }) {
        return () => vue.h(props.as, { class: 'offcanvas-title' }, slots.default && slots.default());
    },
});

exports.COffcanvasTitle = COffcanvasTitle;
//# sourceMappingURL=COffcanvasTitle.js.map
