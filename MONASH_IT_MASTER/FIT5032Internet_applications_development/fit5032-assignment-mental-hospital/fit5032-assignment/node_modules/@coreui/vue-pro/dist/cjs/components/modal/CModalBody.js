'use strict';

var vue = require('vue');

const CModalBody = vue.defineComponent({
    name: 'CModalBody',
    setup(_, { slots }) {
        return () => vue.h('div', { class: 'modal-body' }, slots.default && slots.default());
    },
});

exports.CModalBody = CModalBody;
//# sourceMappingURL=CModalBody.js.map
