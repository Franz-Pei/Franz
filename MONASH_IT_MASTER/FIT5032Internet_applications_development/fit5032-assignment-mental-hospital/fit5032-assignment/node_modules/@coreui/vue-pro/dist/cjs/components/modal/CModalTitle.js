'use strict';

var vue = require('vue');

const CModalTitle = vue.defineComponent({
    name: 'CModalTitle',
    props: {
        /**
         * Component used for the root node. Either a string to use a HTML element or a component.
         */
        as: {
            type: String,
            default: 'h5',
        },
    },
    setup(props, { slots }) {
        return () => vue.h(props.as, { class: 'modal-title' }, slots.default && slots.default());
    },
});

exports.CModalTitle = CModalTitle;
//# sourceMappingURL=CModalTitle.js.map
