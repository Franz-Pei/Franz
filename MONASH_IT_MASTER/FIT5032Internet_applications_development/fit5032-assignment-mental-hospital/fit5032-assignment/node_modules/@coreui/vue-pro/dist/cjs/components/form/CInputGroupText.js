'use strict';

var vue = require('vue');

const CInputGroupText = vue.defineComponent({
    name: 'CInputGroupText',
    props: {
        /**
         * Component used for the root node. Either a string to use a HTML element or a component.
         */
        as: {
            type: String,
            default: 'span',
        },
    },
    setup(props, { slots }) {
        return () => vue.h(props.as, { class: 'input-group-text' }, slots.default && slots.default());
    },
});

exports.CInputGroupText = CInputGroupText;
//# sourceMappingURL=CInputGroupText.js.map
