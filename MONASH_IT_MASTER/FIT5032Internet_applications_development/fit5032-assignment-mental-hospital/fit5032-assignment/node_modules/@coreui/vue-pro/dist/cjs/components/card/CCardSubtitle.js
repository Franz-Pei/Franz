'use strict';

var vue = require('vue');

const CCardSubtitle = vue.defineComponent({
    name: 'CCardSubtitle',
    props: {
        /**
         * Component used for the root node. Either a string to use a HTML element or a component.
         */
        as: {
            type: String,
            default: 'h6',
        },
    },
    setup(props, { slots }) {
        return () => vue.h(props.as, { class: 'card-subtitle' }, slots.default && slots.default());
    },
});

exports.CCardSubtitle = CCardSubtitle;
//# sourceMappingURL=CCardSubtitle.js.map
