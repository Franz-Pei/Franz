'use strict';

var vue = require('vue');

const CNavbarToggler = vue.defineComponent({
    name: 'CNavbarToggler',
    setup(_, { slots }) {
        return () => vue.h('button', {
            class: 'navbar-toggler',
        }, slots.default ? slots.default() : vue.h('span', { class: ['navbar-toggler-icon'] }));
    },
});

exports.CNavbarToggler = CNavbarToggler;
//# sourceMappingURL=CNavbarToggler.js.map
