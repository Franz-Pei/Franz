'use strict';

var vue = require('vue');

const CHeaderText = vue.defineComponent({
    name: 'CHeaderText',
    setup(_, { slots }) {
        return () => vue.h('span', { class: 'header-text' }, slots.default && slots.default());
    },
});

exports.CHeaderText = CHeaderText;
//# sourceMappingURL=CHeaderText.js.map
