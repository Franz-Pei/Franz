'use strict';

var vue = require('vue');

const CHeaderNav = vue.defineComponent({
    name: 'CHeaderNav',
    props: {
        /**
         * Component used for the root node. Either a string to use a HTML element or a component.
         */
        as: {
            type: String,
            default: 'ul',
        },
    },
    setup(props, { slots }) {
        return () => vue.h(props.as, {
            class: 'header-nav',
            role: 'navigation',
        }, slots.default && slots.default());
    },
});

exports.CHeaderNav = CHeaderNav;
//# sourceMappingURL=CHeaderNav.js.map
