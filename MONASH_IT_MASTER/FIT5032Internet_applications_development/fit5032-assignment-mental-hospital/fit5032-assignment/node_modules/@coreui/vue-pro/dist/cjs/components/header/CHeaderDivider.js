'use strict';

var vue = require('vue');

const CHeaderDivider = vue.defineComponent({
    name: 'CHeaderDivider',
    setup(_, { slots }) {
        return () => vue.h('div', { class: 'header-divider' }, slots.default && slots.default());
    },
});

exports.CHeaderDivider = CHeaderDivider;
//# sourceMappingURL=CHeaderDivider.js.map
