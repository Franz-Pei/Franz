'use strict';

var vue = require('vue');

const CNavbarText = vue.defineComponent({
    name: 'CNavbarText',
    setup(_, { slots }) {
        return () => vue.h('span', { class: 'navbar-text' }, slots.default && slots.default());
    },
});

exports.CNavbarText = CNavbarText;
//# sourceMappingURL=CNavbarText.js.map
