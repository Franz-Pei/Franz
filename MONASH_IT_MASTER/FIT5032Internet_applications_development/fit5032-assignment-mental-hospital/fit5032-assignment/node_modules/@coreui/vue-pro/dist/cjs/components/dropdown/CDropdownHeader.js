'use strict';

var vue = require('vue');

const CDropdownHeader = vue.defineComponent({
    name: 'CDropdownHeader',
    props: {
        /**
         * Component used for the root node. Either a string to use a HTML element or a component.
         */
        as: {
            type: String,
            default: 'h6',
        },
    },
    setup(props, { slots }) {
        return () => vue.h(props.as, {
            class: 'dropdown-header',
        }, slots.default && slots.default());
    },
});

exports.CDropdownHeader = CDropdownHeader;
//# sourceMappingURL=CDropdownHeader.js.map
