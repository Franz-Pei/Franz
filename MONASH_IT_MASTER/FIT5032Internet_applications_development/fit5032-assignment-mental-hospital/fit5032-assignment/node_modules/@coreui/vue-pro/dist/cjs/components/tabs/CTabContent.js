'use strict';

var vue = require('vue');

const CTabContent = vue.defineComponent({
    name: 'CTabContent',
    setup(_, { slots }) {
        return () => vue.h('div', { class: 'tab-content' }, slots.default && slots.default());
    },
});

exports.CTabContent = CTabContent;
//# sourceMappingURL=CTabContent.js.map
