'use strict';

var vue = require('vue');

const CNavGroupItems = vue.defineComponent({
    name: 'CNavGroupItems',
    setup(_, { slots }) {
        return () => vue.h('ul', { class: 'nav-group-items' }, slots.default && slots.default());
    },
});

exports.CNavGroupItems = CNavGroupItems;
//# sourceMappingURL=CNavGroupItems.js.map
