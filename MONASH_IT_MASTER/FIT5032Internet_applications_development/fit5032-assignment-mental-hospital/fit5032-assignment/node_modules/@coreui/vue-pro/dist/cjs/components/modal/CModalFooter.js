'use strict';

var vue = require('vue');

const CModalFooter = vue.defineComponent({
    name: 'CModalFooter',
    setup(_, { slots }) {
        return () => vue.h('div', { class: 'modal-footer' }, slots.default && slots.default());
    },
});

exports.CModalFooter = CModalFooter;
//# sourceMappingURL=CModalFooter.js.map
