'use strict';

var vue = require('vue');

const CToastBody = vue.defineComponent({
    name: 'CToastBody',
    setup(_, { slots }) {
        return () => vue.h('div', { class: 'toast-body' }, slots.default && slots.default());
    },
});

exports.CToastBody = CToastBody;
//# sourceMappingURL=CToastBody.js.map
