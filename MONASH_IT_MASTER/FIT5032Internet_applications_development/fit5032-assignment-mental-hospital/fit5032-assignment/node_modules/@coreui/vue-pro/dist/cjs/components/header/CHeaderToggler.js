'use strict';

var vue = require('vue');

const CHeaderToggler = vue.defineComponent({
    name: 'CHeaderToggler',
    setup(_, { slots }) {
        return () => vue.h('button', {
            class: 'header-toggler',
            type: 'button',
            'aria-label': 'Toggle navigation',
        }, slots.default ? slots.default() : vue.h('span', { class: ['header-toggler-icon'] }));
    },
});

exports.CHeaderToggler = CHeaderToggler;
//# sourceMappingURL=CHeaderToggler.js.map
