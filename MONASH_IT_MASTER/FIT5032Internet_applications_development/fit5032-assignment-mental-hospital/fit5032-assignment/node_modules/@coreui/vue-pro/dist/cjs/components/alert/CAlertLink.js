'use strict';

var vue = require('vue');

const CAlertLink = vue.defineComponent({
    name: 'CAlertLink',
    setup(_, { slots }) {
        return () => vue.h('a', {
            class: 'alert-link',
        }, slots);
    },
});

exports.CAlertLink = CAlertLink;
//# sourceMappingURL=CAlertLink.js.map
