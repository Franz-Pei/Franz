'use strict';

var vue = require('vue');

const CNavTitle = vue.defineComponent({
    name: 'CNavTitle',
    props: {
        /**
         * Component used for the root node. Either a string to use a HTML element or a component.
         */
        as: {
            type: String,
            default: 'li',
        },
    },
    setup(props, { slots }) {
        return () => vue.h(props.as, { class: 'nav-title' }, slots.default && slots.default());
    },
});

exports.CNavTitle = CNavTitle;
//# sourceMappingURL=CNavTitle.js.map
