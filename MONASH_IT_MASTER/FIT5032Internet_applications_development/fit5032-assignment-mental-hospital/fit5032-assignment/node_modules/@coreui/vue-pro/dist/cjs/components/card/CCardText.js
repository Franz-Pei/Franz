'use strict';

var vue = require('vue');

const CCardText = vue.defineComponent({
    name: 'CCardText',
    props: {
        /**
         * Component used for the root node. Either a string to use a HTML element or a component.
         */
        as: {
            type: String,
            default: 'p',
        },
    },
    setup(props, { slots }) {
        return () => vue.h(props.as, { class: 'card-text' }, slots.default && slots.default());
    },
});

exports.CCardText = CCardText;
//# sourceMappingURL=CCardText.js.map
