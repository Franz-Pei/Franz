'use strict';

var vue = require('vue');

const CCardTitle = vue.defineComponent({
    name: 'CCardTitle',
    props: {
        /**
         * Component used for the root node. Either a string to use a HTML element or a component.
         */
        as: {
            type: String,
            default: 'h5',
        },
    },
    setup(props, { slots }) {
        return () => vue.h(props.as, { class: 'card-title' }, slots.default && slots.default());
    },
});

exports.CCardTitle = CCardTitle;
//# sourceMappingURL=CCardTitle.js.map
