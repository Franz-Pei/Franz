import { App } from 'vue'
import { CMultiSelect } from './CMultiSelect'

const CMultiSelectPlugin = {
  install: (app: App): void => {
    app.component(CMultiSelect.name as string, CMultiSelect)
  },
}

export { CMultiSelectPlugin, CMultiSelect }
