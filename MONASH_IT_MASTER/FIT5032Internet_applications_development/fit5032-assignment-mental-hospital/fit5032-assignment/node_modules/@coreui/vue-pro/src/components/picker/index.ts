import { App } from 'vue'
import { CPicker } from './CPicker'

const CPickerPlugin = {
  install: (app: App): void => {
    app.component(CPicker.name as string, CPicker)
  },
}

export { CPicker, CPickerPlugin }
